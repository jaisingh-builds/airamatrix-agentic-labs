---
description: Show how much of today's budget this participant has used
---

Run `python3 tools/cost-report.py` and report the result in one or two lines:
spend so far, budget, and what is left.

If the command fails because the gateway is unreachable or the key is rejected,
say which, and point at `setup/06-troubleshooting.md`. Do not retry more than once.
