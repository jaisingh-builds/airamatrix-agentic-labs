{
  "permissions": {
    "allow": [
      "Bash(make:*)",
      "Bash(python3 *test*)",
      "Bash(node --test*)",
      "Bash(mvn -q test*)",
      "Bash(git status)",
      "Bash(git diff*)",
      "Read(**)"
    ],
    "deny": [
      "Read(./.env)",
      "Bash(aws iam *)",
      "Bash(curl * /key/generate*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/block-secrets.sh"
          }
        ]
      }
    ]
  }
}
