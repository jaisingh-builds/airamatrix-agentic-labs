# The runtime's own dependencies - installed for linux/arm64 by `capstone_aws.py deploy` into out/build/,
# never into your machine. Local mode needs none of these (standard library only).
bedrock-agentcore==1.23.1
aws-opentelemetry-distro==0.20.0
boto3>=1.43
