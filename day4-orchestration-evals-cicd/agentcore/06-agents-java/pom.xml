<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Step 6 in Java: the three agents as ONE container image for AgentCore Runtime
       (ROLE picks investigator / reviewer / supervisor). Direct code deploy has no Java
       runtime, so Java agents run as an arm64 container: port 8080, POST /invocations, GET /ping.
       Jib builds and pushes the image - no Docker needed. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.airamatrix</groupId>
  <artifactId>agentcore-agents-java</artifactId>
  <version>1.0.0</version>
  <name>AgentCore step 6 - Java agents (container)</name>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <aws.sdk.version>2.55.6</aws.sdk.version>
    <mcp.version>2.0.1</mcp.version>
    <adot.version>2.31.0</adot.version>
    <otel.version>1.49.0</otel.version>
    <!-- set on the command line: -Dimage=<account>.dkr.ecr.<region>.amazonaws.com/<repo>:<tag> -->
    <image>aira-agents-java:local</image>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>software.amazon.awssdk</groupId>
        <artifactId>bom</artifactId>
        <version>${aws.sdk.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>bedrockruntime</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>bedrockagentcore</artifactId>
    </dependency>
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>apache-client</artifactId>
    </dependency>
    <dependency>
      <groupId>io.modelcontextprotocol.sdk</groupId>
      <artifactId>mcp</artifactId>
      <version>${mcp.version}</version>
    </dependency>
    <!-- Compile against the OpenTelemetry API only; at run time the ADOT Java agent in the image
         provides the SDK and exports the spans. Without the agent (tests, laptop) the API is a no-op. -->
    <dependency>
      <groupId>io.opentelemetry</groupId>
      <artifactId>opentelemetry-api</artifactId>
      <version>${otel.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>aira-agents</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <!-- put the ADOT Java agent where Jib copies it into the image -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>otel-agent</id>
            <phase>package</phase>
            <goals><goal>copy</goal></goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>software.amazon.opentelemetry</groupId>
                  <artifactId>aws-opentelemetry-agent</artifactId>
                  <version>${adot.version}</version>
                  <destFileName>aws-opentelemetry-agent.jar</destFileName>
                </artifactItem>
              </artifactItems>
              <outputDirectory>${project.build.directory}/otel/otel</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.google.cloud.tools</groupId>
        <artifactId>jib-maven-plugin</artifactId>
        <version>3.5.2</version>
        <configuration>
          <from>
            <image>eclipse-temurin:21-jre</image>
            <platforms>
              <platform><architecture>arm64</architecture><os>linux</os></platform>
            </platforms>
          </from>
          <to><image>${image}</image></to>
          <extraDirectories>
            <paths>
              <path><from>${project.build.directory}/otel</from><into>/</into></path>
              <path><from>src/main/jib</from><into>/</into></path>
            </paths>
          </extraDirectories>
          <container>
            <ports><port>8080</port></ports>
            <!-- start.sh configures the ADOT Java agent from what the Runtime injects, then execs java -->
            <entrypoint>
              <arg>/bin/sh</arg>
              <arg>/opt/aira/start.sh</arg>
            </entrypoint>
            <creationTime>USE_CURRENT_TIMESTAMP</creationTime>
          </container>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
